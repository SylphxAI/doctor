name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  release:
    uses: SylphxAI/.github/.github/workflows/release.yml@main
    permissions:
      contents: write
      pull-requests: write
    secrets: inherit
