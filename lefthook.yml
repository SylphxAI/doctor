# Managed by @sylphx/doctor
# https://github.com/evilmartians/lefthook

pre-commit:
  commands:
    doctor:
      run: bun src/cli.ts precommit --fix

pre-push:
  commands:
    doctor:
      run: bun src/cli.ts prepush
